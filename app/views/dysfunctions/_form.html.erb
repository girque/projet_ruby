<%= form_for(@dysfunction) do |f| %>
  <% if @dysfunction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dysfunction.errors.count, "error") %> prohibited this dysfunction from being saved:</h2>

      <ul>
      <% @dysfunction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
